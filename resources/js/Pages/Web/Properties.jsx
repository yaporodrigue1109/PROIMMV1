import { useMemo,useState } from 'react'; import PublicLayout from './PublicLayout'; import PropertyCards from './PropertyCards';
export default function Properties({properties=[],mode=''}) { const [type,setType]=useState(mode||'all'); const filtered=useMemo(()=>type==='all'?properties:properties.filter(p=>p.mode===type),[properties,type]); return <PublicLayout><main className="mx-auto max-w-7xl px-5 py-14"><p className="font-medium text-[#00559b]">Nos annonces</p><h1 className="text-4xl font-bold">Trouvez le bien qui vous ressemble</h1><div className="my-8 flex gap-3">{[['all','Tous'],['location','À louer'],['vente','À vendre']].map(([v,l])=><button key={v} onClick={()=>setType(v)} className={`rounded-lg px-4 py-2 ${type===v?'bg-[#00559b] text-white':'bg-white border'}`}>{l}</button>)}</div><PropertyCards properties={filtered}/></main></PublicLayout> }
