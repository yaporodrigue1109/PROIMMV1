import { Link } from '@inertiajs/react';
export default function PropertyCards({ properties=[] }) { return <div className="grid gap-5 md:grid-cols-2 lg:grid-cols-3">{properties.map(p=><article key={p.id} className="overflow-hidden rounded-2xl border bg-white shadow-sm"><div className="flex h-40 items-center justify-center bg-gradient-to-br from-[#00559b] to-sky-400 text-lg font-semibold text-white">{p.mode==='location'?'À LOUER':'À VENDRE'}</div><div className="p-5"><p className="text-xs font-semibold uppercase text-[#00559b]">{p.mode==='location'?'Location':'Vente'}</p><h3 className="mt-1 font-semibold">{p.title}</h3><p className="mt-1 text-sm text-slate-500">{p.address || 'Adresse sur demande'}</p><div className="mt-4 flex items-center justify-between"><b>{new Intl.NumberFormat('fr-FR').format(p.price)} FCFA{p.mode==='location'?' / mois':''}</b><span className="text-sm text-slate-500">{p.surface ? `${p.surface} m²` : ''}</span></div></div></article>)}{!properties.length&&<p className="col-span-full rounded-xl border border-dashed p-10 text-center text-slate-500">Aucun bien disponible pour le moment.</p>}</div> }
